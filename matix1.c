#include <stdio.h>
#include <string.h>
#include <ctype.h>

	void main() {
	  char text1[100];
	  printf("Please enter strings:");
	  gets(text1);
	   while (strlen(text1) >= 1){
	if(strchr(text1,"bbb")!= NULL)
	  printf("The string is : %s\n",text);
		 gets(text);
	}
}
